package com.example.lostfoundMS.entities.enums;

public enum CustodyStatus {
    WITH_FINDER,        // the person who found it still has it
    AT_FRONT_DESK,      // dropped off at the school's physical collection point
    RETURNED_TO_OWNER   // handed back, item lifecycle complete
}
